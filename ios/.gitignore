# XcodeGen generates the project from project.yml — regenerate with `xcodegen generate`.
*.xcodeproj
# Build output / derived data
build/
DerivedData/
.DS_Store
# SwiftPM (KaleidoEngine package) build artifacts
.build/
Package.resolved
